{% from "@base-button" import button %}
{%- call button(url, class, attrs) -%}
   {% if icon %}
      {% if text %}<span class="button__text">{{ text }}</span>{% endif %}
      {{ icon | icon }}
   {% else -%}
      {{ text }}
   {%- endif %}
{%- endcall -%}
